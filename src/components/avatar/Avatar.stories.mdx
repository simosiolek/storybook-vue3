import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import Avatar from './Avatar.vue';

<Meta 
  title="Atoms/Avatar"
  argTypes= {{
    size: {
      description: 'Select size of avatar',
      control: { type: 'select' },
      options: ['small', 'regular'],
      
      table: {
        category: 'Colors',
        defaultValue: {
          summary: 'regular'
        },
        type: {
          summary: 'The label contents',
          detail: 'small | regular'
        }
      }
    },
  }}
  component={Avatar}
/>

export const Template = (args) => ({
  components: { Avatar },
  setup() {
    return { args };
  },
  template: '<Avatar v-bind="args" />',
});

# Avatar

Let's define a story for our `Avatar` component:

<Story 
  name="Primary" 
  args={{}}>
  {Template.bind({})}
</Story>

Component table
<ArgsTable of={Avatar} />

Story table
<ArgsTable story="Primary" />

We can drop it in a `Canvas` to get a code snippet:

<Canvas>
  <Story 
    name="Secondary" 
    args={{
      alt: true,
      title: 'SM'
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

We can even preview multiple Stories in a block. This
gets rendered as a group but defines individual stories
with unique URLs, which is great for review and testing.

<Canvas columns={2} withSource="open" withToolbar>
  <h2>Can add some extra content to Canvas</h2>
  <Story 
    name="Alt" 
    args={{
      alt: true,
      title: 'SM'
    }}>
    {Template.bind({})}
  </Story>
  <Story 
    name="Default" 
    args={{}}>
    {Template.bind({})}
  </Story>
  <Story 
    name="Small"
    args={{
      title: 'JRR',
      size: 'small'
  }}>
  {Template.bind({})}
  </Story>
</Canvas>